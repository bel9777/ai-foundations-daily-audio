# AGENTS.md

All agent rules for this repo live in **CLAUDE.md** — read that first, then
`docs/current-state.md` for the live handoff. The `app/` Next.js code is the
retired ChatGPT-side pipeline; the live pipeline is `podcast.py`.
